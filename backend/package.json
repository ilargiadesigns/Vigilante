bash

cat /home/claude/vigilante-repo/backend/package.json
Salida

{
  "name": "vigilante-backend",
  "version": "0.3.0",
  "description": "Backend de Vigilante: cámaras reales (SCT) + pipeline de sucesos geolocalizados (RSS + geocodificación + dedup)",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "fast-xml-parser": "^4.4.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
